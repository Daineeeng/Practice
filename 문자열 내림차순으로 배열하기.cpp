#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
using namespace std;

string solution(string s) {
    ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    sort(s.rbegin(), s.rend());
    string answer = s;
    return answer;
}
